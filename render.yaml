services:
  - type: web
    name: love-music-frontend
    env: static
    buildCommand: npm install && CI=false npm run build
    staticPublishPath: ./build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
