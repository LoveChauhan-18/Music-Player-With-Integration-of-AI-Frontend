services:
  - type: web
    name: pittu-music-frontend
    env: static
    buildCommand: npm install && CI=false npm run build
    staticPublishPath: ./build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
